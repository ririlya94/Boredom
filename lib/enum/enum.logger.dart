/// [LoggerLevel]s to control logging output. Logging can be enabled to include all
/// levels above certain [LoggerLevel].
enum LoggerLevel {
  verbose,
  debug,
  info,
  warning,
  error,
  wtf,
}
